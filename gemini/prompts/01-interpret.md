# Phase 1 — interpret source

Read `skills/pbir-reading.md` and `skills/data-honesty.md`, `work/inventory.json`, and relevant PBIP/PBIR files under `input/`. Do not modify output. Write valid JSON to `work/interpretation.json` with: `project`, `pages` (ordered; each with source and visuals), `filters`, `dataMappings`, `limitations`, and `sourcePaths`. Map each visual to its source path and required fields. Distinguish native SQL results from the final Power Query table: inventory all M steps after `Value.NativeQuery`, especially joins to other queries, computed fields, renames, and type conversions. Mark any missing transformed field unavailable unless reconstructed and checked. If data is absent, explicitly mark values unavailable. Be concise but complete; do not invent data or unsupported DAX results.
