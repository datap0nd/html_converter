# Phase 3 — build dynamic HTML

Read `skills/data-honesty.md`, `skills/visual-qa.md`, `work/interpretation.json`, `work/interpretation-review.json`, and the local report/data source as needed. Improve `output/dynamic/index.html` into a useful interactive report. `output/dynamic/report-data.json` is the normalized local export and must stay valid. Preserve `<script type="application/json" id="embedded-report-data">__EMBEDDED_REPORT_DATA__</script>` exactly; data loading must prefer embedded JSON when present and otherwise use only `fetch('./report-data.json')`. Inline all CSS and JS in `index.html`; no CDN or local assets. Use SVG/canvas/HTML if needed. Reconstruct visuals and working page/filter controls where data and metadata support them. Native SQL result rows have not had subsequent Power Query steps applied; if implementing a transformation in client-side JavaScript, trace the M expression precisely and check representative values. Do not assume an unprovided merged query, such as a color lookup, is available. Unsupported visuals or missing values must show an explicit placeholder, not fake figures. Preserve the visible report-status warning. Do not write to `input/` or run shell commands. Write valid JSON to `work/build-notes.json` with `implemented`, `placeholders`, `limitations`, and `sourcePaths`.
